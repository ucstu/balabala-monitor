{
  "name": "@balabala/monitor-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "lerna run build && lerna bootstrap && vite",
    "build": "lerna run build && lerna bootstrap && vue-tsc --noEmit && vite build",
    "preview": "lerna run build && lerna bootstrap && vite preview"
  },
  "dependencies": {
    "@balabala/api": "^1.0.0",
    "font-awesome": "^4.7.0",
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.0",
    "sass": "^1.54.0",
    "typescript": "^4.6.4",
    "vite": "^3.0.0",
    "vue-tsc": "^0.38.4"
  }
}
